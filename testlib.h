#include<iostream>
using namespace std;
int testcase(int l, int m, int z, int n)
{
	if (m != z || z != m){
		cout << "Wrong dimensions for multiplications" << endl<<"Run program again"<<endl;
		return 1;
	}
}
